import type { Metadata } from 'next'
import './globals.css'
import Navbar from '@/components/Navbar'
import Footer from '@/components/Footer'

export const metadata: Metadata = {
  title: 'Integrain Foundation | Rooted in Integrity, Enabling a Better Society',
  description: 'Integrain Foundation empowers socially underprivileged, economically underserved, and politically underrepresented communities by providing access to essential resources, education, and advocacy.',
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body className="flex flex-col min-h-screen">
        <Navbar />
        <main className="flex-grow">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  )
}
